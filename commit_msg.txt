Replace placeholder circle with the official Google "G" logo

The "Sign in/up with Google" buttons were rendering an empty gray donut
SVG (single <circle opacity="0.2" />) which read as a broken/loading
icon. Swap it for the official 4-color Google "G" brand mark on both
Login.tsx and Signup.tsx, add aria-label and aria-hidden for screen
readers, and tag the SVG with a CSS hook for any future styling.

Verification:
- npm run build: 4897 modules transformed, 31.6s, exit 0
- New chunk hash: index-CzI1ntfi.js (different from previous build)